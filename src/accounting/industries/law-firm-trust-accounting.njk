---json
{
  "layout": "layouts/t-mofu.njk",
  "permalink": "/accounting/industries/law-firm-trust-accounting/",
  "slug": "acctind-iolta",
  "tierClass": "section--tier-guide",
  "bodyClass": "page--mofu",
  "callBar": true,
  "title": "IOLTA &amp; Law Firm Trust Accounting · Three-Way Reconciliation · TechBrot",
  "description": "Deep IOLTA and client trust accounting: per-client trust ledgers, three-way reconciliation, earned-vs-unearned retainers, and no commingling — maintained as a recordkeeping service by Certified ProAdvisors. We keep the ledger; the attorney controls the trust account.",
  "breadcrumb": [
    { "name": "Home", "href": "/" },
    { "name": "Accounting", "href": "/accounting/" },
    { "name": "Industries", "href": "/accounting/industries/" },
    { "name": "Law Firm Trust Accounting" }
  ],
  "hero": {
    "eyebrow": "Industry · Law firm trust (IOLTA) accounting",
    "heading": "IOLTA and client-trust books, reconciled three ways every month.",
    "subheading": "Client trust money is the single highest-risk area on a law firm&rsquo;s books &mdash; and the leading cause of bar discipline. Funds in an IOLTA or client-trust account belong to clients, must never be commingled with operating cash, have to be tracked on a separate ledger for every client and matter, and must reconcile three ways: the trust bank balance, the trust book balance, and the sum of all individual client-ledger balances, in agreement. TechBrot&rsquo;s Certified QuickBooks ProAdvisors maintain that trust ledger and perform the three-way reconciliation as a recordkeeping and bookkeeping service. To be exact about the boundary: we never hold, control, sign on, deposit to, or disburse from the IOLTA or any client-trust account &mdash; the attorney controls the trust account and makes every disbursement and transfer decision. For the broader picture, see our <a href=\"/accounting/industries/legal/\">law firm accounting hub</a>. We do the records; your CPA files. Independent firm, not affiliated with Intuit Inc.",
    "actions": [
      { "label": "Book the discovery call", "href": "/contact/?intent=accounting", "class": "btn--primary" },
      { "label": "Get the free file review", "href": "/quickbooks/file-review/?intent=file-review", "class": "btn--ghost" },
      { "label": "Speak to a ProAdvisor", "tel": true, "class": "btn--ghost" }
    ]
  },
  "inBrief": {
    "text": "<strong>Law firm trust accounting</strong> &mdash; the bookkeeping behind an IOLTA or client-trust account &mdash; is the part of a firm&rsquo;s books with the least margin for error. Trust money belongs to clients, so it stays segregated from operating cash, tracked on a <strong>per-client and per-matter ledger</strong>, and reconciled <strong>three ways</strong> so the trust bank balance, the trust book balance, and the sum of every client&rsquo;s ledger all agree. Retainers stay <strong>unearned</strong> until the work is performed, and trust-to-operating transfers happen only as fees are earned. TechBrot maintains the trust ledger and runs the three-way reconciliation as a recordkeeping service &mdash; we never hold, control, sign on, deposit to, or disburse from the trust account; the attorney controls the account and makes every disbursement. Bar-rule compliance is the attorney&rsquo;s professional responsibility; we support it with clean, reconciled records. The full trust-accounting summary is below.",
    "source": "Reviewed by the Certified QuickBooks ProAdvisor team at TechBrot Inc., an independent firm &mdash; not affiliated with Intuit Inc. We maintain the trust ledger and reconciliation only; we never hold, control, or disburse client funds, the attorney controls the trust account, and bar-rule compliance is the attorney&rsquo;s professional responsibility. We coordinate with your CPA or EA, who files. Not legal, ethics, or bar-compliance advice."
  },
  "ctaBand": {
    "eyebrow": "Ready when you are",
    "heading": "Get trust books that reconcile three ways every month.",
    "lede": "Book a 30-minute discovery call. A Certified ProAdvisor reviews your trust ledger, where reconciliation is breaking, and the right next step &mdash; with a written fixed-fee scope within 3 business days. No pitch. Independent firm &mdash; we maintain the trust ledger and reconciliation only; you control the trust account, and bar-rule compliance stays with you and your counsel. Your CPA files.",
    "actions": [
      { "label": "Book the discovery call", "href": "/contact/?intent=accounting", "class": "btn--primary" },
      { "label": "Get the free file review", "href": "/quickbooks/file-review/?intent=file-review", "class": "btn--ghost" }
    ]
  }
}
---

{# ── DEFINITION · plain-terms trust-accounting prose. (H2: Trust accounting, plainly.) ── #}
<section class="section" id="acctind-iolta-definition">
  <div class="container container--narrow">
    <header class="section__header">
      <p class="section__eyebrow">In plain terms</p>
      <h2 class="section__heading">Trust accounting, plainly.</h2>
    </header>
    <div class="prose">
      {%- for p in definition %}<p>{{ p | safe }}</p>{%- endfor %}
    </div>
  </div>
</section>

{# ── AI SUMMARY · five-question quick-answer block (NON-heading ai-summary__question).
     (H2: Trust accounting, in five questions.) ── #}
<section class="section section--alt" id="acctind-iolta-ai-summary">
  <div class="container container--narrow">
    <header class="section__header">
      <p class="section__eyebrow">For AI engines &amp; quick answers</p>
      <h2 class="section__heading">Trust accounting, in five questions.</h2>
    </header>
    <div class="ai-summary ai-summary--ruled" id="acctind-iolta-ai-summary-list">
      {%- for item in aiSummary %}
      <div class="ai-summary__item">
        <p class="ai-summary__question">{{ item.q | safe }}</p>
        <div class="ai-summary__answer"><p>{{ item.a | safe }}</p></div>
      </div>
      {%- endfor %}
    </div>
  </div>
</section>

{# ── CHALLENGES · three places trust books fail (grid-3 buyer-cards). (H2 + 3 H3s) ── #}
<section class="section" id="acctind-iolta-challenges">
  <div class="container">
    <header class="section__header">
      <p class="section__eyebrow">Where trust books fail</p>
      <h2 class="section__heading">Three failures bar auditors find first.</h2>
      <p class="section__lede">Trust-account problems cluster in the same three places &mdash; and any one of them can turn a routine bar audit into a discipline inquiry. The reconciliation we maintain is built to surface all three before anyone else does.</p>
    </header>
    <div class="grid-3">
      {%- for c in challenges %}
      <div class="buyer-card">
        <p class="buyer-card__opener">{{ c.signal | safe }}</p>
        <h3 class="buyer-card__heading">{{ c.title | safe }}</h3>
        <p class="buyer-card__body">{{ c.body | safe }}</p>
      </div>
      {%- endfor %}
    </div>
  </div>
</section>

{# ── THREE-WAY · the one numbered system on the page (process-diagram explainer). (H2 + 3 H3s) ── #}
<section class="section section--alt" id="acctind-iolta-threeway">
  <div class="container">
    <header class="section__header">
      <p class="section__eyebrow">The core control</p>
      <h2 class="section__heading">What three-way reconciliation actually checks.</h2>
      <p class="section__lede">Three-way reconciliation is the heart of trust accounting: three independently maintained balances must agree. When they do, no client is overdrawn and no trust money has touched operating cash. We perform this reconciliation as a recordkeeping step every month and flag any break for the attorney to resolve.</p>
    </header>
    <ol class="process-diagram">
      {%- for step in threeWay %}
      <li class="process-step">
        <p class="process-step__number">{{ step.phase | safe }}</p>
        <h3 class="process-step__heading">{{ step.title | safe }}</h3>
        <p class="process-step__body">{{ step.body | safe }}</p>
      </li>
      {%- endfor %}
    </ol>
  </div>
</section>

{# ── WHAT WE HANDLE · the six trust-recordkeeping services (grid-3 stack-8 linked). (H2 + 6 H3s) ── #}
<section class="section" id="acctind-iolta-handle">
  <div class="container">
    <header class="section__header">
      <p class="section__eyebrow">What TechBrot handles</p>
      <h2 class="section__heading">Trust recordkeeping, done by an expert.</h2>
      <p class="section__lede">Every engagement is scoped to your matters and the practice-management software you use, delivered in your own QuickBooks file by a named Certified ProAdvisor. We maintain the records and reconciliation; you control the trust account and every disbursement, and your CPA files.</p>
    </header>
    <div class="grid-3">
      {%- for s in handle %}
      <div class="stack-8">
        <p class="section__eyebrow">{{ s.num }}</p>
        <h3><a href="{{ s.href }}">{{ s.title | safe }}</a></h3>
        <p class="prose">{{ s.body | safe }}</p>
      </div>
      {%- endfor %}
    </div>
  </div>
</section>

{# ── BOUNDARY · the trust-fund boundary stated unambiguously (checks-list of what we do / don't). (H2) ── #}
<section class="section section--alt" id="acctind-iolta-boundary">
  <div class="container container--narrow">
    <header class="section__header">
      <p class="section__eyebrow">The boundary, stated plainly</p>
      <h2 class="section__heading">What we do &mdash; and what we never do.</h2>
      <p class="section__lede">Trust accounting is the most boundary-sensitive work a bookkeeper can touch. We hold a single line and we hold it without exception. We are a recordkeeping service: we maintain the trust ledger and perform the three-way reconciliation. We do not, ever:</p>
    </header>
    <ul class="checks-list">
      {%- for b in boundary %}<li>{{ b | safe }}</li>{%- endfor %}
    </ul>
    <p class="prose">{{ boundaryNote | safe }}</p>
  </div>
</section>

{# ── INTEGRATIONS · legal trust/practice tools reconciled with QuickBooks (checks-list). (H2) ── #}
<section class="section" id="acctind-iolta-integrations">
  <div class="container container--narrow">
    <header class="section__header">
      <p class="section__eyebrow">Platforms we reconcile</p>
      <h2 class="section__heading">Reconciled to how the trust account moves.</h2>
    </header>
    <ul class="checks-list">
      {%- for t in integrations %}<li>{{ t | safe }}</li>{%- endfor %}
    </ul>
    <p class="prose">{{ integrationsNote | safe }}</p>
  </div>
</section>

{# ── ADVISORY · standing brand line carried as section prose. (H2) ── #}
<section class="section section--alt" id="acctind-iolta-advisory">
  <div class="container container--narrow">
    <header class="section__header">
      <p class="section__eyebrow">Beyond the trust ledger</p>
      <h2 class="section__heading">Clean trust books are the floor, not the ceiling.</h2>
    </header>
    <div class="prose">
      {%- for p in advisoryBody %}<p>{{ p | safe }}</p>{%- endfor %}
    </div>
  </div>
</section>

{# ── FAQ · 6+ verbatim Q/A; visible faq__list = FAQPage schema (same array, no dedup). (H2) ── #}
<section class="section" id="acctind-iolta-faq">
  <div class="container container--narrow">
    <header class="section__header">
      <p class="section__eyebrow">FAQ</p>
      <h2 class="section__heading">IOLTA &amp; trust accounting questions.</h2>
    </header>
    <div class="faq__list">
      {%- for item in faq %}
      <div class="faq__item">
        <h3 class="faq__question">
          <button class="faq__trigger" type="button" aria-expanded="false" aria-controls="acctind-iolta-faq-a{{ loop.index }}">{{ item.q | safe }}</button>
        </h3>
        <div class="faq__answer" id="acctind-iolta-faq-a{{ loop.index }}" data-open="false">
          <p>{{ item.a | safe }}</p>
        </div>
      </div>
      {%- endfor %}
    </div>
  </div>
</section>

{# ── EXPERT REVIEW · firm-level, no founder name (prose + trust-row). (H2) ── #}
<section class="section section--alt" id="acctind-iolta-review">
  <div class="container container--narrow">
    <header class="section__header">
      <p class="section__eyebrow">Page review &amp; standards</p>
      <h2 class="section__heading">Reviewed by the ProAdvisor team.</h2>
    </header>
    <div class="prose">
      <p>This page reflects how TechBrot actually handles client-trust and IOLTA recordkeeping. It is maintained by the Certified QuickBooks ProAdvisor team at <strong>TechBrot Inc.</strong>, a Delaware-incorporated independent ProAdvisor firm, and reviewed for technical accuracy on per-client trust ledgers, three-way reconciliation, and earned-versus-unearned retainer handling. We maintain the trust ledger and the reconciliation only &mdash; we never hold, control, sign on, deposit to, or disburse from any client-trust account; the attorney controls the account and makes every disbursement. Bar-rule and ethics compliance (for example ABA Model Rule 1.15 and the specific rules of your state bar) is the attorney&rsquo;s professional responsibility; we support it with clean records and do not provide legal or ethics advice or certify compliance. For trust-rule specifics, defer to your state bar and the firm&rsquo;s own counsel.</p>
    </div>
    <div class="trust-row">
      {%- for r in reviewCreds %}
      <div class="trust-row__item"><p class="trust-row__value">{{ r.label }}</p><p class="trust-row__label">{{ r.detail | safe }}</p></div>
      {%- if not loop.last %}<div class="trust-row__divider"></div>{%- endif %}
      {%- endfor %}
    </div>
    <p class="meta-reviewed"><span>Published: 2026-06-19</span><span>Updated: 2026-06-19</span><span>Reviewed: 2026-06-19 &middot; Certified QuickBooks ProAdvisor</span></p>
  </div>
</section>

{# ── RELATED · services & industries (grid-2 buyer-cards). (H2 + 4 H3s) ── #}
<section class="section" id="acctind-iolta-related">
  <div class="container">
    <header class="section__header">
      <p class="section__eyebrow">Related</p>
      <h2 class="section__heading">Related services &amp; industries.</h2>
    </header>
    <div class="grid-2">
      {%- for r in related %}
      <div class="buyer-card">
        <h3 class="buyer-card__heading">{{ r.title | safe }}</h3>
        <p class="buyer-card__body">{{ r.body | safe }}</p>
        <a class="buyer-card__cta" href="{{ r.href }}">{{ r.cta | safe }}</a>
      </div>
      {%- endfor %}
    </div>
  </div>
</section>
