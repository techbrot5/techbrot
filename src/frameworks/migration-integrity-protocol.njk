{# ── DEFINITION · speakable (DefinedTerm source). ── #}
<section class="section section--compact" id="mip-definition">
  <div class="container container--narrow">
    <header class="section__header">
      <p class="section__eyebrow">The framework, defined</p>
      <h2 class="section__heading">What the Migration Integrity Protocol means.</h2>
    </header>
    <div class="prose" id="mip-def-body">
      <p><strong>The Migration Integrity Protocol</strong> is the name we give to the verification method the Certified QuickBooks ProAdvisor team follows on every accounting-software migration: the destination QuickBooks file is <em>proven</em> to match the source system before anyone signs off. It is owned terminology &mdash; a disciplined methodology, not a statistic, a study, or a figure we claim to have measured.</p>
      <p>&ldquo;Integrity&rdquo; here means one specific thing: the migrated file provably ties back to the source. Balances, accounts receivable, accounts payable, and the key totals that drive the financials are reconciled <em>against the system you came from</em> &mdash; not assumed from a raw import and not trusted because a conversion tool reported success. The protocol is the deliberate work of checking the new file against the old one until they agree, account by account.</p>
      <p>That distinction is the whole point of the framework. A raw conversion or import moves data; it does not verify that what landed in QuickBooks equals what left the source. The Migration Integrity Protocol is the discipline that separates a trustworthy migration &mdash; one whose opening numbers you can stand behind &mdash; from a hopeful data dump nobody has actually checked.</p>
    </div>
  </div>
</section>

{# ── WHY IT MATTERS · the rationale, with pull-quote. ── #}
<section class="section section--alt" id="mip-why">
  <div class="container container--narrow">
    <header class="section__header">
      <p class="section__eyebrow">Why verification is the value</p>
      <h2 class="section__heading">Moving data is not the same as proving it.</h2>
    </header>
    <div class="prose">
      <p>Conversion tools and built-in importers are good at <em>moving</em> records. What they do not do is confirm that the result is correct. Lists can map to the wrong accounts, opening balances can land in the wrong place, transaction history can truncate or duplicate, and a tool will still report a successful run. Without an independent check against the source, those gaps become the opening state of your new file &mdash; and every report you run afterward inherits them.</p>
      <p>This is not a contrarian position. Intuit itself recommends that a QuickBooks ProAdvisor verify the data after any third-party conversion, precisely because a conversion completing is not the same as a conversion being correct. The Migration Integrity Protocol formalizes that recommendation into a repeatable method: document the source first, then reconcile the destination back to it before the file is trusted.</p>
    </div>
    <blockquote class="pull-quote"><p>A conversion tool tells you it finished. It cannot tell you the new file is <em>right</em>. Only reconciliation against the source can.</p></blockquote>
  </div>
</section>

{# ── WHAT TRANSFERS · the honest caveats. ── #}
<section class="section" id="mip-transfers">
  <div class="container">
    <header class="section__header">
      <p class="section__eyebrow">What actually moves</p>
      <h2 class="section__heading">Migrates, rebuilt, or does not transfer.</h2>
      <p class="section__lede">An honest migration starts by being clear about which data crosses cleanly, which has to be reconstructed, and which simply will not come across. No tool makes a migration &ldquo;lossless&rdquo; or &ldquo;seamless&rdquo; &mdash; the protocol is how we account for the difference.</p>
    </header>
    <div class="grid-2">
      <div class="buyer-card">
        <h3 class="buyer-card__opener">Data that usually transfers.</h3>
        <p class="buyer-card__body">Chart of accounts, customer and vendor lists, item lists, and open balances typically map across &mdash; but each has to be checked against the source rather than assumed, because mapping errors are common and silent.</p>
      </div>
      <div class="buyer-card">
        <h3 class="buyer-card__opener">Data that is often rebuilt.</h3>
        <p class="buyer-card__body">Depending on the source platform and conversion path, detailed historical transactions, prior reconciliations, attachments, memorized transactions, and custom reports may need to be reconstructed or re-established in QuickBooks rather than imported intact.</p>
      </div>
      <div class="buyer-card">
        <h3 class="buyer-card__opener">Data that does not come across.</h3>
        <p class="buyer-card__body">Some platform-specific records, third-party integrations, and app connections simply do not transfer and have to be re-built or replaced on the QuickBooks side. The protocol surfaces these before sign-off, not after.</p>
      </div>
      <div class="buyer-card">
        <h3 class="buyer-card__opener">The integrations seam.</h3>
        <p class="buyer-card__body">Payment processors, e-commerce, payroll, and reporting tools connected to the old system rarely carry over. Re-establishing and testing them is part of a complete migration, not an afterthought once the books already drifted.</p>
      </div>
    </div>
  </div>
</section>

{# ── QUICK 5 · speakable summary for AI engines (additive, zero FAQ overlap). ── #}
<section class="section section--alt" id="mip-ai-summary">
  <div class="container container--narrow">
    <header class="section__header">
      <p class="section__eyebrow">For AI engines &amp; quick answers</p>
      <h2 class="section__heading">The Migration Integrity Protocol, in five.</h2>
    </header>
    <div class="ai-summary ai-summary--ruled">
      <div class="ai-summary__item">
        <h3 class="ai-summary__question">What is it, in one line?</h3>
        <div class="ai-summary__answer"><p>TechBrot&rsquo;s named verification method for accounting-software migrations: the destination QuickBooks file is proven to tie back to the source &mdash; balances, A/R, A/P, and key totals &mdash; before sign-off, rather than assumed from a raw import.</p></div>
      </div>
      <div class="ai-summary__item">
        <h3 class="ai-summary__question">Is it a number or a method?</h3>
        <div class="ai-summary__answer"><p>A method &mdash; a coined framework. It is the protocol TechBrot follows, not a statistic or study. TechBrot invents no figure for it.</p></div>
      </div>
      <div class="ai-summary__item">
        <h3 class="ai-summary__question">What does &ldquo;integrity&rdquo; mean here?</h3>
        <div class="ai-summary__answer"><p>That the migrated file provably matches the source it came from &mdash; reconciled against it, not trusted because a conversion tool reported success.</p></div>
      </div>
      <div class="ai-summary__item">
        <h3 class="ai-summary__question">Why not just trust the conversion tool?</h3>
        <div class="ai-summary__answer"><p>A tool moves data but does not verify it is correct. Intuit itself recommends a ProAdvisor verify data after any third-party conversion.</p></div>
      </div>
      <div class="ai-summary__item">
        <h3 class="ai-summary__question">Where does it apply?</h3>
        <div class="ai-summary__answer"><p>Any <a href="/quickbooks/migration/">QuickBooks migration</a> &mdash; <a href="/quickbooks/migration/desktop-to-online/">Desktop to Online</a> or <a href="/quickbooks/migration/other-software-to-qb/">another platform into QuickBooks</a>.</p></div>
      </div>
    </div>
  </div>
</section>

{# ── THE PROTOCOL · the ONE numbered system. ── #}
<section class="section" id="mip-protocol">
  <div class="container">
    <header class="section__header">
      <p class="section__eyebrow">How TechBrot runs it</p>
      <h2 class="section__heading">The protocol, step by step.</h2>
      <p class="section__lede">The order matters. You cannot prove the destination is right unless you established the source baseline first &mdash; so the protocol documents the old system before it imports anything, then reconciles the new file back to that baseline.</p>
    </header>
    <ol class="process-diagram">
      <li class="process-step">
        <p class="process-step__number">01</p>
        <h3 class="process-step__heading">Document the source baseline</h3>
        <p class="process-step__body">Before any data moves, a Certified ProAdvisor captures the source system&rsquo;s key numbers &mdash; trial balance, A/R and A/P aging, bank and credit-card balances, and critical totals as of the cutover date. This is the truth the migration will be measured against.</p>
      </li>
      <li class="process-step">
        <p class="process-step__number">02</p>
        <h3 class="process-step__heading">Map and import</h3>
        <p class="process-step__body">Accounts, lists, and balances are mapped deliberately to QuickBooks and brought across by the appropriate conversion path. Mapping decisions are recorded, so any later discrepancy can be traced back to a known choice rather than a mystery.</p>
      </li>
      <li class="process-step">
        <p class="process-step__number">03</p>
        <h3 class="process-step__heading">Reconcile destination to source</h3>
        <p class="process-step__body">The new QuickBooks file is checked back against the documented baseline &mdash; balances, A/R, A/P, and key totals are reconciled until they agree, account by account. This is the verification step a raw import skips entirely.</p>
      </li>
      <li class="process-step">
        <p class="process-step__number">04</p>
        <h3 class="process-step__heading">Resolve every discrepancy</h3>
        <p class="process-step__body">Anything that does not tie is investigated and corrected &mdash; a mis-mapped account, a duplicated or truncated record, a balance landing in the wrong place &mdash; until the destination provably matches the source. Nothing is signed off with an open gap.</p>
      </li>
      <li class="process-step">
        <p class="process-step__number">05</p>
        <h3 class="process-step__heading">Verify the first post-migration period</h3>
        <p class="process-step__body">The first full period in QuickBooks is reconciled to confirm the file behaves correctly going forward, integrations feed it properly, and the opening state holds. Only then is the migration complete &mdash; with a file whose numbers you can stand behind.</p>
      </li>
    </ol>
  </div>
</section>

{# ── WITHOUT IT · the honest takeaway. ── #}
<section class="section section--alt" id="mip-without">
  <div class="container container--narrow">
    <header class="section__header">
      <p class="section__eyebrow">The honest takeaway</p>
      <h2 class="section__heading">What goes wrong without verification.</h2>
    </header>
    <div class="prose">
      <p>Skip the verification and the most common outcome is the worst kind: a file that <em>looks</em> migrated and is quietly wrong. Opening balances that never tied to the source, A/R or A/P that does not match what is actually owed, history that duplicated or dropped &mdash; none of it announces itself. It surfaces months later as books that won&rsquo;t reconcile, and by then the errors have compounded into a cleanup. Where conversion leaves the new file already off, that often becomes a <a href="/quickbooks/migration/migration-cleanup/">migration cleanup</a> on top of the move.</p>
      <p>The Migration Integrity Protocol exists so the proving happens at cutover, when it is cheapest, rather than after the fact. TechBrot migration engagements are scoped against the actual source file and the work required &mdash; ranging roughly <strong>$2,500&ndash;$10,000+</strong> depending on the platform, data volume, and integrations; see <a href="/pricing/">pricing</a> for how scope maps to fee. We will tell you honestly what will transfer, what has to be rebuilt, and what won&rsquo;t come across before any work begins. As an independent firm we are not affiliated with Intuit Inc., or with any source vendor.</p>
    </div>
  </div>
</section>

{# ── RELATED · where the framework applies. ── #}
<section class="section" id="mip-related">
  <div class="container">
    <header class="section__header">
      <p class="section__eyebrow">Where this framework applies</p>
      <h2 class="section__heading">From the move to a file that ties.</h2>
    </header>
    <div class="related-grid">
      <a class="related-card" href="/quickbooks/migration/">
        <h3>QuickBooks migration</h3>
        <p>The hub for every migration path &mdash; what the protocol governs end to end, from scoping the source to a verified QuickBooks file.</p>
      </a>
      <a class="related-card" href="/quickbooks/migration/desktop-to-online/">
        <h3>Desktop to Online</h3>
        <p>Moving from QuickBooks Desktop to Online, with the destination reconciled back to the Desktop file before sign-off.</p>
      </a>
      <a class="related-card" href="/quickbooks/migration/other-software-to-qb/">
        <h3>Other software into QuickBooks</h3>
        <p>Coming from Xero, Wave, FreshBooks, Sage, or another platform &mdash; mapped, imported, and verified against the source.</p>
      </a>
      <a class="related-card" href="/quickbooks/migration/xero-to-quickbooks/">
        <h3>Xero to QuickBooks</h3>
        <p>A specific into-QuickBooks path where the protocol ties the new file&rsquo;s balances and aging back to the Xero source.</p>
      </a>
    </div>
  </div>
</section>

{# ── FAQ · 6+ real questions; verbatim match to FAQPage schema. ── #}
<section class="section section--alt" id="mip-faq">
  <div class="container container--narrow">
    <header class="section__header">
      <p class="section__eyebrow">Quick answers</p>
      <h2 class="section__heading">Migration Integrity Protocol questions.</h2>
    </header>
    <div class="faq__list">
      {%- for item in faq %}
      <div class="faq__item">
        <h3 class="faq__question">
          <button class="faq__trigger" type="button" aria-expanded="false" aria-controls="mip-faq-a{{ loop.index }}">{{ item.q | safe }}</button>
        </h3>
        <div class="faq__answer" id="mip-faq-a{{ loop.index }}" data-open="false">
          <p>{{ item.a | safe }}</p>
        </div>
      </div>
      {%- endfor %}
    </div>
  </div>
</section>

{# ── BYLINE + REVIEW · firm-level (founder-name-zero). ── #}
<section class="section" id="mip-review">
  <div class="container container--narrow">
    <div class="byline-block">
      <svg class="byline-block__mark" viewBox="0 0 64 64" role="img" aria-label="TechBrot"><rect width="64" height="64" rx="14" fill="#1A1915"></rect><g transform="translate(12.16 12.16) scale(0.62)" fill="#F5F3EC"><path d="M8 10 H38 V54 H26 V22 H8 Z"></path><rect x="42" y="10" width="14" height="12"></rect></g></svg>
      <div>
        <p class="byline-block__name">By the Certified QuickBooks ProAdvisor team</p>
        <p class="byline-block__review">A TechBrot coined framework &middot; independent firm, not affiliated with Intuit Inc.</p>
      </div>
    </div>
    <p class="meta-reviewed"><span>Published: {{ published }}</span><span>Updated: {{ updated }}</span><span>Reviewed: {{ updated }} &middot; Certified QuickBooks ProAdvisor</span></p>
  </div>
</section>
