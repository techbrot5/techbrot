---json
{
  "layout": "layouts/t-hub.njk",
  "permalink": "/quickbooks/online/integrations/",
  "slug": "qbo-integrations-hub",
  "showBadges": true,
  "title": "QuickBooks Online Integrations Setup Guide",
  "description": "Connect Stripe, PayPal, Square, Shopify, Bill.com, Expensify, Gusto, HubSpot, and QuickBooks Payments to QBO right. Most sync net of fees, so mapping matters.",
  "breadcrumb": [
    { "name": "Home", "href": "/" },
    { "name": "QuickBooks", "href": "/quickbooks/" },
    { "name": "Online", "href": "/quickbooks/online/" },
    { "name": "Integrations" }
  ],
  "heroFigure": false,
  "hero": {
    "eyebrow": "QuickBooks Online · Integrations",
    "heading": "Connecting an app to QuickBooks is easy. Reconciling it is where the books break.",
    "subheading": "Connecting an app to QuickBooks takes a few clicks — reconciling it is where books break, since most integrations post net of fees and must be mapped right. This hub routes to a plain-English page for all nine QBO integrations. Independent firm — not Intuit.",
    "actions": [
      { "label": "Get the free file review", "href": "/quickbooks/file-review/?intent=file-review", "class": "btn--primary" },
      { "label": "Book the discovery call", "href": "/contact/?intent=quickbooks", "class": "btn--ghost" }
    ],
    "trust": [
      "Certified QuickBooks ProAdvisor team",
      "Independent · not Intuit · not these apps",
      "We set up and reconcile the data flow in your file"
    ]
  },
  "ctaBand": {
    "eyebrow": "Start with your real file",
    "heading": "We'll set up the integration — and make it reconcile.",
    "lede": "Get a free QuickBooks file review or book a discovery call. We'll look at how your apps connect to QBO today — what's mapped where, what's posting net of fees, and what isn't reconciling — and set it up so the books actually balance. No pitch. Independent firm — not Intuit.",
    "actions": [
      { "label": "Get the free file review", "href": "/quickbooks/file-review/?intent=file-review", "class": "btn--primary" },
      { "label": "Book the discovery call", "href": "/contact/?intent=quickbooks", "class": "btn--ghost" }
    ]
  },
  "sec": {
    "summaryId": "qbo-integrations-hub-summary",
    "summaryTag": "In short",
    "summaryH": "All nine QuickBooks Online integrations, and where each one breaks the books.",
    "faqTag": "FAQ",
    "faqH": "QuickBooks Online integrations questions."
  }
}
---

{# ── INTEGRATION GRID · grouped by workflow, 9 QBO integration pages (dc-card hub-cards). (H2 per group.) ── #}
{%- for group in integrationGroups %}
<section class="sec reveal{% if loop.index is even %} hub-rt--alt{% endif %}" id="qbo-integrations-hub-{{ group.id }}"{% if loop.index is even %} style="background:var(--color-paper-warm);border-top:1px solid var(--border-subtle);border-bottom:1px solid var(--border-subtle);"{% endif %}>
  <div class="ct">
    <div class="idx"><span class="idx__n">§</span><span class="idx__t">{{ group.eyebrow | safe }}</span><span class="idx__rule"></span></div>
    <h2 class="h2" style="max-width:28ch;margin-bottom:{% if group.lede %}10{% else %}28{% endif %}px;">{{ group.heading | safe }}</h2>
    {%- if group.lede %}<p class="lede" style="margin:0 0 30px;max-width:66ch;">{{ group.lede | safe }}</p>{%- endif %}
    <div class="r-3">
      {%- for f in group.integrations %}
      <a class="dc-card hub-card" href="{{ f.href }}"><h3>{{ f.title | safe }}</h3><p class="prose" style="font-size:14.5px;margin:6px 0 0;color:var(--color-ink-mute);">{{ f.body | safe }}</p><span class="hub-card__go">{{ f.cta | safe }}</span></a>
      {%- endfor %}
    </div>
  </div>
</section>
{%- endfor %}

{# ── CROSS-LINKS (read). ── #}
<section class="sec reveal" id="qbo-integrations-hub-related">
  <div class="ct read">
    <p class="prose">{{ relatedNote | safe }}</p>
  </div>
</section>
