Native `<select>` styled to match Input, with a hairline chevron.

```jsx
<Select options={["QuickBooks Online", "Xero", "NetSuite", "Other"]} defaultValue="Xero" />
```

Accepts strings or `{value, label}` objects. Wrap in `<Field>` for a label.
