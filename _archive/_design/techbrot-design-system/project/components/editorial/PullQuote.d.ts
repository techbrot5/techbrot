/**
 * Pull-quote — Fraunces statement between ink and hairline rules,
 * woven into long-form guide prose.
 */
export interface PullQuoteProps {
  children: React.ReactNode;
  /** Uppercase attribution, e.g. "TechBrot methodology". */
  attribution?: string;
}
