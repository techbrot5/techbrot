Disclosure — the locked "We are not Intuit" independence banner. Mandatory on every QuickBooks-related page; ships with the canonical statement by default.

```jsx
<Disclosure />
```

Override `children` only for page-specific legal copy — never soften the independence claim.
