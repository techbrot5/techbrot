Phone CTA — expert-framed phone unit. The label is always about talking to a Certified ProAdvisor, never a "support line" or "hotline".

```jsx
<PhoneCta number="(555) 014-0220" detail="Mon–Fri, 9am–6pm ET · a ProAdvisor answers, not a queue" />
```

Set `inverted` inside dark bands. Replace the placeholder number with the firm's real line.
