Framework — DefinedTerm card for a coined term the silo owns (term + definition + firm attribution). Used on hub pages and guides to anchor citations.

```jsx
<Framework term="The Reconciliation Standard">
  Every account reconciled to source, every balance supported by a document,
  every adjustment explained in plain language — before a file is called clean.
</Framework>
```
