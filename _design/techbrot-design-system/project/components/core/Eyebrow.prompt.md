Uppercase tracked topic label that sits above section headings — terse, 2–4 words ("What we do", "Independent disclosure").

```jsx
<Eyebrow>How it works</Eyebrow>
```

Set `inverted` on dark bands. This is the ONLY place uppercase is allowed in the system.
