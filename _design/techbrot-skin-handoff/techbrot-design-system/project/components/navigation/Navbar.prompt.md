Sticky marketing-site header — the only blur/transparency in the system. Horizontal logo left, centered links, ink CTA right.

```jsx
<Navbar active="Research" onCta={() => openContact()} />
```

Default links: Services · Methodology · Research · About. Pass `onNavigate` for SPA-style prototypes.
