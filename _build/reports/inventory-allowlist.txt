# inventory-drift allowlist — built pages intentionally EXCLUDED from sitemap.xml.
# These are canonicalized-away duplicates (their canonicalUrl != own URL), so sitemap.njk
# drops them on purpose. Any built-not-in-sitemap URL NOT listed here FAILS the
# `inventory-drift` battery check (a new orphan = real drift to investigate, not allowlist).
# One URL per line, leading-slash, trailing-slash form.
/resources/checklists/quickbooks-setup-checklist/
