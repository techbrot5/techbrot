name = "techbrot"
main = "functions"
compatibility_date = "2024-11-01"
compatibility_flags = ["nodejs_compat"]

# Enable Cloudflare Browser Rendering API
browser = { binding = "BROWSER" }

# Cron (existing)
[triggers]
crons = ["0 * * * *"]
